<%= form_for(current_user.user_favorites.build, remote: true) do |f| %>
  <div><% hidden_field_tag :favmicropost_id, @user.user_favorites %></div>
  <%= @user.user_favorites.inspect %>
  <%= f.submit "Favorite", class: "btn btn-primary" %>
<% end %>