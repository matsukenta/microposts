<div class="row">
    <aside class="col-md-4">
        <section class="user_info">
        <h1>
            <%= gravatar_for @user %>
            <%= @user.name %>
        </h1>
        </section>
        【プロフィール】
        <%= simple_format(@user.profile) %>
    </aside>
    <div class="col-md-8">
        <!-- このビューにアクセスしたときにログインしていたら「follow」「unfollow」ボタンを表示 -->
        <%= render 'follow_form' if logged_in? %>
        <br />
        
        <ul class="nav nav-tabs">
            <li><a href="<%= user_path %>">Microposts (<%= @user.microposts.count %>)</a></li>
            <li><a href="<%= following_user_path %>">Following (<%= @user.following_users.count %>)</a></li>
            <li><a href="<%= followers_user_path %>">Followers (<%= @user.follower_users.count %>)</a></li>
            <li class="active"><a href="<%= favorites_path %>">Favorite (<%= @user.favorite_microposts.count %>) </a></li>
        </ul>

        <div class="tab-content">
            <!-- お気に入り投稿の一覧表示 -->
            <% if @user.user_favorites.any? %>
                <br />
                <ol class="microposts">
                    <%= render partial: 'users/favorite', collection: @favorites %>
                    <%# @followers.inspect %>
                </ol>
            <% end %>
        </div>
    </div>
</div>